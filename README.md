# Dots

```bash
git clone --recurse-submodules git@github.com:darthshittious/dots.git dotfiles
cd dotfiles

# Install all packages
paru -S --needed - < pkglist.txt

# Deploy dots
./installer.sh install
```
